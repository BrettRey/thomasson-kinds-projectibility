# Thomasson Projectibility Revision Intake
## Proposed decisions
Adopt:

- Define `deflation` near the introduction so the subtitle doesn't suggest crude eliminativism.

- Add one roadmap sentence tying the four levels to sections 2--6.

- Flag the `London` example as anticipating the SFL discussion, rather than moving it.

- Shorten the introductory field definition; leave fuller field machinery in section 5.

- Use `ordinary linguistic practice` when the field-relativity claim is doing argumentative work.

- Change `support grades` to `graded support`.

- Strengthen the conclusion's `extra discipline` sentence.

- Soften the species example to avoid implying every field literally settles specieshood in the same sense.

- Qualify `institutional uptake` as relevant especially for human or social kinds.

- Run a mechanical spacing/proof pass against the PDF text and bibliography.


Adapt:

- Keep the title for now, but make the subtitle pressure safer by defining `deflation`.

- Keep section 3's size; no expansion.

- Leave the Dupré/Boyd order unless a small local edit is needed while revising.


Reject:

- No wholesale reorganization.

- No casual title change in this pass.

## Targeted source changes
1. Introduction:

  - Add a one-sentence definition of `deflation`.

  - Shorten the field definition.

  - Add a roadmap sentence using the four-level distinction.

  - Change the lead-in to the `London` example to say it anticipates section 3.

2. Section 4:

  - Change the species example from `count together as species` to `grouped together for ecological purposes and distinguished for genetic purposes`.

3. Section 5:

  - Use `graded support` instead of `support grades`.

  - Qualify `institutional uptake`.

4. Conclusion:

  - Use `ordinary linguistic practice`.

  - Replace `hasn't yet made that extra discipline unnecessary` with `hasn't yet shown that this discipline can be absorbed into conceptual engineering alone`.

5. Verification:

  - Central style check.

  - Paragraph-length guard.

  - Terminology/acronym guard.

  - PDF text spacing scan.

  - Bibliography spacing scan.

  - `git diff --check`.

  - `make quick`.
